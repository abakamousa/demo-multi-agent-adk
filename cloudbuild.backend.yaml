substitutions:
  _IMAGE: us-central1-docker.pkg.dev/$PROJECT_ID/demo-adk/backend

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - Dockerfile.backend
      - -t
      - ${_IMAGE}:latest
      - .

images:
  - ${_IMAGE}:latest
