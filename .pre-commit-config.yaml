repos:
  - repo: local
    hooks:
      - id: detect-secrets
        name: detect secrets
        entry: uv run --extra dev detect-secrets-hook
        language: system
        types: [text]
        exclude: ^(\.secrets\.baseline|uv\.lock)$
